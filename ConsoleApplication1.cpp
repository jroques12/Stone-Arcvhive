// ConsoleApplication1.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include <string>
#include <vector>
#include <array>
#include <algorithm>
#include <fstream>
#include "TryAgain.h"
using namespace std;

vector<Stone> dataBase;
int main()
{


//playing with these code lines to debug related functions
	// once debugged will use Switch statement to ask whether the user wants to 1. View Database, 2.Add to Database, 3. Modify(including deleting) an entry, 4. Search by name, keyword, or color 



	/*
	Stone userQuery =searchArchive(dataBase);
	cout << "You entered: " << dataBase[userQuery.vecPosition].stoneName << endl;
	cout << "The keywords associated with it are: " << dataBase[userQuery.vecPosition].keyWord << endl;
	cout << "Known colors are: " << dataBase[userQuery.vecPosition].color << endl;
	
	
	int stoneCount = 0;
	
	for (unsigned int i = 0; i < dataBase.size(); i++)
	{
		cout << dataBase[i].stoneName << endl;
		cout << dataBase[i].keyWord << endl;
		cout << dataBase[i].color << endl;
		cout << "----------------------------" << endl;
		stoneCount++;
	}
	cout << "There are " << stoneCount << " stones in the Archive." << endl;
	system("pause");
	endUserInit();
	displayAttempt();
	stoneModAttempt();
	*/
	system("pause");
	return 0;
}
